[package]
name = "volume30-3022"
version = "0.1.0"
edition = "2024"

[dependencies]
proconio = { version = "0.5.0", features = ["derive"] }
block-cut-tree = { path = "../../../graph/block-cut-tree" }
