[package]
name = "persistent_unionfind_rollback"
version = "0.1.0"
edition = "2024"

[dependencies]
fast-io = { path = "../../../../macro/fast-io" }
union-find-with-rollback = { path = "../../../../data-structure/union-find/union-find-with-rollback" }
