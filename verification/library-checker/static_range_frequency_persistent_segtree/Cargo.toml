[package]
name = "static_range_frequency_persistent_segtree"
version = "0.1.0"
edition = "2024"

[dependencies]
proconio = { version = "0.5.0", features = ["derive"] }
persistent-segment-tree = { path = "../../../data-structure/persistent-segment-tree" }
