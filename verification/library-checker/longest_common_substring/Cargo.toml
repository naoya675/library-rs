[package]
name = "longest_common_substring"
version = "0.1.0"
edition = "2024"

[dependencies]
proconio = { version = "0.5.0", features = ["derive"] }
suffix-array = { path = "../../../string/suffix-array" }
lcp-array = { path = "../../../string/lcp-array" }
