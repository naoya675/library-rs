[package]
name = "tree_diameter-rerooting"
version = "0.1.0"
edition = "2024"

[dependencies]
itertools = { path = "../../../macro/itertools" }
proconio = { version = "0.5.0", features = ["derive"] }
rerooting = { path = "../../../tree/rerooting" }
