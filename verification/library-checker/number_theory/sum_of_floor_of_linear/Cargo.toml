[package]
name = "sum_of_floor_of_linear"
version = "0.1.0"
edition = "2024"

[dependencies]
proconio = { version = "0.5.0", features = ["derive"] }
floor-sum = { path = "../../../../math/floor-sum" }
